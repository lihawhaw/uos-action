export type InputsType = {
  secretId: string;
  secretKey: string;
  bucket: string;
  region: string;
  localPath: string;
  remotePath: string;
  uploadOrderSuffix: string;
  cdnDomain: string;
  cdnRefresh: boolean;
  cdnRefreshSuffix: string;
}
